// Todas las llamadas usan AppConfig.baseUrl y requieren FirebaseAuth Token.

ENDPOINT: /crearReserva (POST)
Body: { userId, groupClassId }

ENDPOINT: /cancelarReserva (POST)
Body: { bookingId, userId }

ENDPOINT: /registrarFichaje (POST)
Body: { userId, type ('IN'/'OUT'), wifiSsid, deviceId, manualTime? }

ENDPOINT: /enviarCodigoOTP (POST)
Body: { userId, documentId }